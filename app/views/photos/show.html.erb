<h1>Photo</h1>

<% if @photos %>
  <% @photos.each do |photo| %>
    <div class="panel panel-default">
      <div class="panel-heading preview">
        <%= image_tag photo.image %>
      </div>
      <div class="panel-body">
        <%= link_to "Delete", photo, class: "btn btn-default", method: :delete, remote: true, data: { confirm: "Are you sure?" } %>
      </div>
    </div>
  <% end %>
<% end %>
