<div class="categories-overview">
  <div class="category-intro">
    <h2>Categoriën</h2>
    <p><%= link_to "+ Voeg nieuwe categorie toe", new_admin_category_path %></p>
  </div>

  <div class="categories-container">
    <h2>Overzicht</h2>
    <ul>
    <% @categories.each do |category| %>
        <li>
          <p class="name"><%=link_to category.name, admin_category_path(category) %></p>
          <p class="edit"><%= link_to "Edit", edit_admin_category_path(category) %></p>
          <p class="delete"><%= link_to "Delete", admin_category_path(category), method: :delete, data: { confirm: "Weet je het zeker?" } %></p>
        </li>
    <% end %>
  </ul>
  </div>
</div>
